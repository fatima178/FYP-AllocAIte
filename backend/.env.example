DATABASE_URL=

# Optional fallback variables if DATABASE_URL is not set.
ALLOCATE_DB_NAME=allocaite
ALLOCATE_DB_USER=postgres
ALLOCATE_DB_PASSWORD=
ALLOCATE_DB_HOST=localhost
ALLOCATE_DB_PORT=5432

# Comma separated list of frontend origins allowed to call the API.
ALLOWED_ORIGINS=http://localhost:3000

# Used when generating invite links.
FRONTEND_BASE_URL=http://localhost:3000
